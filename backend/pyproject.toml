[project]
name = "minipost-backend"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "fastapi[standard-no-fastapi-cloud-cli]>=0.135.1",
]

[dependency-groups]
dev = [
    "ruff>=0.15.2",
    "ty>=0.0.25",
]
