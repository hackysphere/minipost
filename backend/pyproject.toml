[project]
name = "minipost-backend"
version = "1.0.2"
requires-python = ">=3.14"
dependencies = [
    "argon2-cffi>=25.1.0",
    "fastapi[standard-no-fastapi-cloud-cli]>=0.135.1",
    "pyjwt>=2.12.1",
]

[dependency-groups]
dev = [
    "ruff>=0.15.2",
    "ty>=0.0.25",
]
