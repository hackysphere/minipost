name: minipost

services:
  minipost:
    container_name: minipost
    build: .
    # no image to pull yet
    ports:
      - 8000:8000
    volumes:
      - ./minipost:/app/backend/data
    restart: unless-stopped
